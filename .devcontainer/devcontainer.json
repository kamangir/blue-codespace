{
    "name": "Python & OpenCV",
    "image": "mcr.microsoft.com/vscode/devcontainers/python:3.9",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "postCreateCommand": "apt-get update && apt-get install -y libgl1-mesa-glx && pip install opencv-python-headless && pip install abcli blue-geo blueflow",
    "extensions": [
        "ms-python.python"
    ],
    "remoteUser": "root"
}